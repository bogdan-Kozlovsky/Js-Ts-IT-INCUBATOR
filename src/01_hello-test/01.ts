import {strict} from "assert";

export const sum = (a: number, b: number) => {
    return a + b
}

export function mult(a: number, b: number) {
    return a * b
}

export const splitIntoWords = (sent1:string) => {
    return ['a', 'b', 'c']
}

